apiVersion: apps/v1
kind: Deployment
metadata:
  name: scheduler
  namespace: chis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scheduler
  template:
    metadata:
      labels:
        app: scheduler
    spec:
      containers:
        - name: scheduler
          image: 799491730030.dkr.ecr.us-east-1.amazonaws.com/chis-scheduler:latest
          env:
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: chis-secret
                  key: DB_URL
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: chis-config
                  key: REDIS_URL
